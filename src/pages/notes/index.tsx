import React from "react";
import EmptyState from "@/components/emptystate";

export default function Notes() {
	return (
		<div className="m-[30vh]">
			<EmptyState title="Feature not available at the moment" />
		</div>
	);
}
